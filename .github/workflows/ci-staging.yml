name: CI Staging (Manual Only)
on:
  workflow_dispatch:
    inputs:
      info:
        description: "This is a stub on main. Switch 'Use workflow from' to feature/ci-staging."
        required: false
        type: string

jobs:
  noop:
    # hard block runs on main just in case someone clicks Run here
    if: ${{ github.ref_name != 'main' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is a stub on main. Run this workflow from the feature/ci-staging branch."
