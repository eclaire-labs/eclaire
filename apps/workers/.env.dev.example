# Eclaire Workers - Development Environment Configuration

# Application Environment
NODE_ENV=development
LOG_LEVEL=debug

# Security Configuration
# Allow dev keys (enables insecure development patterns for testing)
# WARNING: NEVER set this to true in production environments
ALLOW_DEV_KEYS=true

# Worker Configuration
WORKER_PORT=3002
WORKER_CONCURRENCY=3
AI_TIMEOUT=180000

# Queue Backend Configuration
# QUEUE_BACKEND can be "redis" (default) or "database"
# Must match the backend's QUEUE_BACKEND setting
# - "redis": Workers connect to Redis/BullMQ queues
# - "database": Workers poll backend API for jobs from database queue
QUEUE_BACKEND=redis
#QUEUE_BACKEND=database

# Redis Configuration (only needed when QUEUE_BACKEND=redis)
REDIS_URL=redis://127.0.0.1:6379

# Backend API Configuration
# Always required - workers call backend APIs for status updates
# In database queue mode, also used for fetching jobs
BACKEND_URL=http://localhost:3001
API_BASE_URL=http://localhost:3001

# External Services
DOCLING_SERVER_URL=http://127.0.0.1:5001

# Data Directories - Centralized
DATA_DIR=../../data
USERS_DIR=../../data/users
LOGS_DIR=../../data/logs
BROWSER_DATA_DIR=../../data/browser-data
CONFIG_DIR=../../config

# Shared Storage Path
WORKER_SHARED_DATA_PATH=../../data/users

# API keys used by workers (DEVONLY - FOR DEVELOPMENT ONLY)
WORKER_API_KEY=sk-DEVONLYWORKER01-DEVONLY0000000000000000000000000
AI_ASSISTANT_API_KEY=sk-DEVONLYAIASST01-DEVONLY1111111111111111111111111

# AI Configuration (Workers) - Active model selection is now in data/config/models.json
# The activeModel.workers setting in JSON config determines which model is used
AI_LOCAL_PROVIDER_URL=http://127.0.0.1:11435
# AI_PROXY_PROVIDER_URL=
# AI_PROXY_API_KEY=
AI_PROMPT_LOGGING_ENABLED=true

# REDDIT_CLIENT_ID=
# REDDIT_CLIENT_SECRET=

# Classic Github PAT, public_repo scope. (optional, for higher rate limits)
# GITHUB_TOKEN=