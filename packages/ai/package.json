{
  "name": "@eclaire/ai",
  "version": "0.0.0",
  "private": true,
  "description": "Unified AI client for Eclaire - multi-provider support with adapters",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "oxlint --tsconfig ../../tsconfig.oxlint.json --max-warnings=0 --report-unused-disable-directives .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "fix": "oxlint --fix --tsconfig ../../tsconfig.oxlint.json . && oxfmt .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "echo 'Usage: pnpm test:integration:local or pnpm test:integration:openrouter' && exit 1",
    "test:integration:local": "AI_TEST_PROVIDER=local vitest run",
    "test:integration:openrouter": "AI_TEST_PROVIDER=openrouter vitest run",
    "test:all": "vitest run && AI_TEST_PROVIDER=local vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "async-mutex": "0.5.0",
    "tiktoken": "1.0.22"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "4.1.5",
    "zod": "4.4.1"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  }
}
