{
  "name": "@eclaire/channels-discord",
  "version": "0.0.0",
  "private": true,
  "description": "Discord channel adapter for Eclaire",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "lint": "oxlint --tsconfig ../../../tsconfig.oxlint.json --max-warnings=0 --report-unused-disable-directives .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "fix": "oxlint --fix --tsconfig ../../../tsconfig.oxlint.json . && oxfmt ."
  },
  "dependencies": {
    "@discordjs/voice": "0.19.2",
    "@eclaire/channels-core": "workspace:*",
    "@eclaire/core": "workspace:*",
    "discord.js": "14.26.3"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "4.1.5",
    "zod": "4.4.1"
  },
  "peerDependencies": {
    "sodium-native": "^4.0.0",
    "zod": "^4.0.0"
  },
  "peerDependenciesMeta": {
    "sodium-native": {
      "optional": true
    }
  }
}
